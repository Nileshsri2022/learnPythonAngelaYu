# 🔧 Quiz Project Part 3: The QuizBrain and the next_question() Method

---

### Overview

**Course:** 100 Days of Code™: The Complete Python Pro Bootcamp
**Chapter:** Day 17 - Intermediate - The Quiz Project & the Benefits of OOP
**Lecture:** Quiz Project Part 3: The QuizBrain and the next_question() Method
**Level:** Intermediate

---

### Summary

Now that we&#x27;ve managed to create a question bank which consists of a list of question objects, the next natural thing to do is to actually bring up one of those questions and to ask the user to answer the question. Now, for all of the questioning and quizzing functionality, we&#x27;re going to put that into the QuizBrain, which is going to be a class in its own right. And it&#x27;s going to manage everything from asking the user for the next question, checking whether if the answer was right and also checking to see if we&#x27;re actually at the end of the quiz. You&#x27;re going to be creating this QuizBrain and class and this class is going to have two attributes to begin with; a question number which is going to have a default value of zero because all our quizzes will start from the first question. And this is going to keep track of which question the user is currently on. And we&#x27;re going to use that number to go through the list of questions which will be passed over to this QuizBrain object when it gets initialized. And then you&#x27;re going to have a method which is called next_question which will pull up the question from that list depending on which current question number we&#x27;re on. If you take a look at the final version of the quiz game, you can see that it displays the question number, Q. and then that number. And then it displays the question text, and then it asks the user for an input, true or false. This is what we&#x27;re going to be focusing on in this lesson. Inside your quiz-game-start, go to the quiz_brain file and create the quiz brain class. Add those two attributes; one is the question number which starts out with a default value and the other is the question list which is going to be initialized when you create a new quiz brain. And we&#x27;re going to be passing over this question bank here into this question list when we initialize a new QuizBrain. Pause the video and see if you can complete this challenge. Right? All right. So let&#x27;s start off by creating our new class which is going to be called QuizBrain and inside this class, we&#x27;ll need to initialize some of those attributes, so I&#x27;m going to create the init method. Inside the init method I&#x27;m going to create that first attribute which was called question_number and I said we would set it to have a default value of zero. So this means every time we create a new QuizBrain object from this class, it&#x27;s ready going to have an attribute called question_number that set to zero. Now the next attribute is called question_list, and this is not going to have a default value. Instead, it&#x27;s going to get it as a value passed over. So lets put that as a q_list here and set it equal to question_list. So what&#x27;s going to happen is inside our main.py file, we&#x27;re going to be initializing that QuizBrain and the value that we&#x27;re going to put in there as the question list is going to be the question bank. This way we&#x27;ll receive the question bank and it will be inside this question list attribute. Now I have another challenge for you. Create a method called next_question inside the QuizBrain. This method needs to retrieve the item at the current question number from the question list. And once you have this item, use the input function to show the user the question text and ask for the user&#x27;s answer. Pause the video and give this a go. Alright, here&#x27;s the solution. First we&#x27;re gonna define a new method called the next_question, and this method is basically going to get hold of the current question and that is of course going to be from the question list. And then we&#x27;re going to tap into that list and get hold of the item at the current question_number. So for example, question_number is going to start out being zero and so it&#x27;s going to go into this list which is basically going to be our list of questions from our main.py, so our question bank. And remember, each of the items inside that list is a question object and question objects have text attributes and answer attributes. So in addition to getting hold of the current question, we can then get hold of the current question text by taking the current question and then just simply writing .text to get the value that&#x27;s saved in that attribute. We&#x27;re going to use an input to ask the user for what their answer is to this question. This is going to be an fstring because we&#x27;re going to be inserting the current_ question.text. And at the very beginning if you remember, we also have the Q. and then the number and then the colon. So that will be the self.question_number. And then we&#x27;ve got our colon and then it&#x27;s going to be the question text. And then we&#x27;re going to ask the user for a input; true or false. And that&#x27;s it. That completes the challenge. Now all that&#x27;s left is to show the very first question to the user and run our code. Let&#x27;s go back to the main.py and import the QuizBrain class. So we&#x27;re going to say from the quiz_brain file import the QuizBrain class. And once we&#x27;ve created our question bank, then the next thing to do is to create our new quiz. So this is going to be a new QuizBrain object and when we initialize it, we have to pass in a list of questions. And that is of course going to be the question bank. So now that we&#x27;ve got our quiz up and running, then we can say quiz.next_question. Let&#x27;s go ahead and run this main.py file. And you can see we get our question text being displayed, true or false being asked, but our question number starts from zero. And that is, of course, because we need zero here in order to get hold of the first question from the list and that&#x27;s why this has a default value of zero. But by the time we reach this input, it would be really good if this actually read one instead of zero. So one way we could do this is we could do this plus one, but remembering that we actually need to increase the question number every single time we call next_question anyways, then once we&#x27;ve gotten hold of the current question, we can actually tap into that self.question_number and increase it by one. This will have the same effect, and if you look at it now we&#x27;ve got the right question number being shown here, the right text being shown, and then we can insert true or false. Now at this stage, our program is just going to exit. In the next lesson, we&#x27;re going to have to work out a way for our questions to keep being asked. And we&#x27;ll need to write some more code for that to work.

---

### Key Concepts

| # | Concept | Description |
|---|---------|-------------|
| 1 | **Class definitions (class)** | Introduced/used in this lecture |
| 2 | **if/elif/else conditionals** | Introduced/used in this lecture |
| 3 | **Module imports** | Introduced/used in this lecture |

---

### 🏋️ Practice Exercise

Now that we&#x27;ve managed to create a question bank which consists of a list of question objects, the next natural thing to do is to actually bring up one of those questions and to ask the user to answer the question. Now, for all of the questioning and quizzing functionality, we&#x27;re going to put that into the QuizBrain, which is going to be a class in its own right. And it&#x27;s going to manage everything from asking the user for the next question, checking whether if the answer was right and also checking to see if we&#x27;re actually at the end of the quiz. You&#x27;re going to be creating this QuizBrain and class and this class is going to have two attributes to begin with; a question number which is going to have a default value of zero because all our quizzes will start from the first question. And this is going to keep track of which question the user is currently on. And we&#x27;re going to use that number to go through the list of questions which will be passed over to this QuizBrain object when it gets initialized. And then you&#x27;re going to have a method which is called next_question which will pull up the question from that list depending on which current question number we&#x27;re on. If you take a look at the final version of the quiz game, you can see that it displays the question number, Q. and then that number. And then it displays the question text, and then it asks the user for an input, true or false. This is what we&#x27;re going to be focusing on in this lesson. Inside your quiz-game-start, go to the quiz_brain file and create the quiz brain class. Add those two attributes; one is the question number which starts out with a default value and the other is the question list which is going to be initialized when you create a new quiz brain. And we&#x27;re going to be passing over this question bank here into this question list when we initialize a new QuizBrain. Pause the video and see if you can complete this challenge. Right? All right. So let&#x27;s start off by creating our new class which is going to be called QuizBrain and inside this class, we&#x27;ll need to initialize some of those attributes, so I&#x27;m going to create the init method. Inside the init method I&#x27;m going to create that first attribute which was called question_number and I said we would set it to have a default value of zero. So this means every time we create a new QuizBrain object from this class, it&#x27;s ready going to have an attribute called question_number that set to zero. Now the next attribute is called question_list, and this is not going to have a default value. Instead, it&#x27;s going to get it as a value passed over. So lets put that as a q_list here and set it equal to question_list. So what&#x27;s going to happen is inside our main.py file, we&#x27;re going to be initializing that QuizBrain and the value that we&#x27;re going to put in there as the question list is going to be the question bank. This way we&#x27;ll receive the question bank and it will be inside this question list attribute. Now I have another challenge for you. Create a method called next_question inside the QuizBrain. This method needs to retrieve the item at the current question number from the question list. And once you have this item, use the input function to show the user the question text and ask for the user&#x27;s answer. Pause the video and give this a go. Alright, here&#x27;s the solution. First we&#x27;re gonna define a new method called the next_question, and this method is basically going to get hold of the current question and that is of course going to be from the question list. And then we&#x27;re going to tap into that list and get hold of the item at the current question_number. So for example, question_number is going to start out being zero and so it&#x27;s going to go into this list which is basically going to be our list of questions from our main.py, so our question bank. And remember, each of the items inside that list is a question object and question objects have text attributes and answer attributes. So in addition to getting hold of the current question, we can then get hold of the current question text by taking the current question and then just simply writing .text to get the value that&#x27;s saved in that attribute. We&#x27;re going to use an input to ask the user for what their answer is to this question. This is going to be an fstring because we&#x27;re going to be inserting the current_ question.text. And at the very beginning if you remember, we also have the Q. and then the number and then the colon. So that will be the self.question_number. And then we&#x27;ve got our colon and then it&#x27;s going to be the question text. And then we&#x27;re going to ask the user for a input; true or false. And that&#x27;s it. That completes the challenge. Now all that&#x27;s left is to show the very first question to the user and run our code. Let&#x27;s go back to the main.py and import the QuizBrain class. So we&#x27;re going to say from the quiz_brain file import the QuizBrain class. And once we&#x27;ve created our question bank, then the next thing to do is to create our new quiz. So this is going to be a new QuizBrain object and when we initialize it, we have to pass in a list of questions. And that is of course going to be the question bank. So now that we&#x27;ve got our quiz up and running, then we can say quiz.next_question. Let&#x27;s go ahead and run this main.py file. And you can see we get our question text being displayed, true or false being asked, but our question number starts from zero. And that is, of course, because we need zero here in order to get hold of the first question from the list and that&#x27;s why this has a default value of zero. But by the time we reach this input, it would be really good if this actually read one instead of zero. So one way we could do this is we could do this plus one, but remembering that we actually need to increase the question number every single time we call next_question anyways, then once we&#x27;ve gotten hold of the current question, we can actually tap into that self.question_number and increase it by one. This will have the same effect, and if you look at it now we&#x27;ve got the right question number being shown here, the right text being shown, and then we can insert true or false. Now at this stage, our program is just going to exit. In the next lesson, we&#x27;re going to have to work out a way for our questions to keep being asked. And we&#x27;ll need to write some more code for that to work.

---

### Next Steps

Now that we&#x27;ve managed to create a question bank which consists of a list of question objects, the next natural thing to do is to actually bring up one of those questions and to ask the user to answer the question. Now, for all of the questioning and quizzing functionality, we&#x27;re going to put that into the QuizBrain, which is going to be a class in its own right. And it&#x27;s going to manage everything from asking the user for the next question, checking whether if the answer was right and also checking to see if we&#x27;re actually at the end of the quiz. You&#x27;re going to be creating this QuizBrain and class and this class is going to have two attributes to begin with; a question number which is going to have a default value of zero because all our quizzes will start from the first question. And this is going to keep track of which question the user is currently on. And we&#x27;re going to use that number to go through the list of questions which will be passed over to this QuizBrain object when it gets initialized. And then you&#x27;re going to have a method which is called next_question which will pull up the question from that list depending on which current question number we&#x27;re on. If you take a look at the final version of the quiz game, you can see that it displays the question number, Q. and then that number. And then it displays the question text, and then it asks the user for an input, true or false. This is what we&#x27;re going to be focusing on in this lesson. Inside your quiz-game-start, go to the quiz_brain file and create the quiz brain class. Add those two attributes; one is the question number which starts out with a default value and the other is the question list which is going to be initialized when you create a new quiz brain. And we&#x27;re going to be passing over this question bank here into this question list when we initialize a new QuizBrain. Pause the video and see if you can complete this challenge. Right? All right. So let&#x27;s start off by creating our new class which is going to be called QuizBrain and inside this class, we&#x27;ll need to initialize some of those attributes, so I&#x27;m going to create the init method. Inside the init method I&#x27;m going to create that first attribute which was called question_number and I said we would set it to have a default value of zero. So this means every time we create a new QuizBrain object from this class, it&#x27;s ready going to have an attribute called question_number that set to zero. Now the next attribute is called question_list, and this is not going to have a default value. Instead, it&#x27;s going to get it as a value passed over. So lets put that as a q_list here and set it equal to question_list. So what&#x27;s going to happen is inside our main.py file, we&#x27;re going to be initializing that QuizBrain and the value that we&#x27;re going to put in there as the question list is going to be the question bank. This way we&#x27;ll receive the question bank and it will be inside this question list attribute. Now I have another challenge for you. Create a method called next_question inside the QuizBrain. This method needs to retrieve the item at the current question number from the question list. And once you have this item, use the input function to show the user the question text and ask for the user&#x27;s answer. Pause the video and give this a go. Alright, here&#x27;s the solution. First we&#x27;re gonna define a new method called the next_question, and this method is basically going to get hold of the current question and that is of course going to be from the question list. And then we&#x27;re going to tap into that list and get hold of the item at the current question_number. So for example, question_number is going to start out being zero and so it&#x27;s going to go into this list which is basically going to be our list of questions from our main.py, so our question bank. And remember, each of the items inside that list is a question object and question objects have text attributes and answer attributes. So in addition to getting hold of the current question, we can then get hold of the current question text by taking the current question and then just simply writing .text to get the value that&#x27;s saved in that attribute. We&#x27;re going to use an input to ask the user for what their answer is to this question. This is going to be an fstring because we&#x27;re going to be inserting the current_ question.text. And at the very beginning if you remember, we also have the Q. and then the number and then the colon. So that will be the self.question_number. And then we&#x27;ve got our colon and then it&#x27;s going to be the question text. And then we&#x27;re going to ask the user for a input; true or false. And that&#x27;s it. That completes the challenge. Now all that&#x27;s left is to show the very first question to the user and run our code. Let&#x27;s go back to the main.py and import the QuizBrain class. So we&#x27;re going to say from the quiz_brain file import the QuizBrain class. And once we&#x27;ve created our question bank, then the next thing to do is to create our new quiz. So this is going to be a new QuizBrain object and when we initialize it, we have to pass in a list of questions. And that is of course going to be the question bank. So now that we&#x27;ve got our quiz up and running, then we can say quiz.next_question. Let&#x27;s go ahead and run this main.py file. And you can see we get our question text being displayed, true or false being asked, but our question number starts from zero. And that is, of course, because we need zero here in order to get hold of the first question from the list and that&#x27;s why this has a default value of zero. But by the time we reach this input, it would be really good if this actually read one instead of zero. So one way we could do this is we could do this plus one, but remembering that we actually need to increase the question number every single time we call next_question anyways, then once we&#x27;ve gotten hold of the current question, we can actually tap into that self.question_number and increase it by one. This will have the same effect, and if you look at it now we&#x27;ve got the right question number being shown here, the right text being shown, and then we can insert true or false. Now at this stage, our program is just going to exit. In the next lesson, we&#x27;re going to have to work out a way for our questions to keep being asked. And we&#x27;ll need to write some more code for that to work.
