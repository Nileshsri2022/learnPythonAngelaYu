# Day 25 Goals- what we will make by the end of the day

---

### 1. Skills Covered on Day 25

* **CSV files** — what they are, and reading them two ways (`csv` module vs. Pandas)
* **Pandas** — the data-analysis library: Series and DataFrames
* Selecting rows/columns, iterating, and writing data back to CSV

---

### 2. The Projects

1. **Squirrel Census analysis** — real data from Central Park: count colours with
   Pandas and output a new CSV.
2. **U.S. States Game** — a Sporcle-style quiz: guess all 50 states on a blank map;
   guesses checked against `50_states.csv`, progress saved to `states_to_learn.csv`.

---

### Summary Checklist

1. CSV = comma-separated values — the universal simple data format.
2. Pandas turns CSVs into queryable tables.
3. Data in → analysis/interactivity → data out.
