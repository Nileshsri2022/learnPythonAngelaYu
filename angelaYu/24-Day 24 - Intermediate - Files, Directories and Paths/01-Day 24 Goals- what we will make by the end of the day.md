# Day 24 Goals- what we will make by the end of the day

---

### 1. Skills Covered on Day 24

* **Reading, writing and closing files** with the `with` keyword
* File modes: read (`"r"`), write (`"w"`), append (`"a"`)
* **Relative vs. absolute paths**
* Lists-of-lines: `readlines()`, `strip()`, `split()`

---

### 2. The Projects

1. **Snake High Score** — the snake game's high score now *survives* closing the
   program by living in a `data.txt` file.
2. **Mail Merge** — read `invited_names.txt`, take `starting_letter.txt`, and generate a
   personalised letter for every guest: a real automation task.

---

### Summary Checklist

1. Files give programs **memory beyond their own runtime**.
2. `with open(...) as file:` handles opening and closing safely.
3. Paths locate files relative to (or absolutely from) the working directory.
