# 🔧 Create a Snake Class & Move to OOP

---

### Overview

**Course:** 100 Days of Code™: The Complete Python Pro Bootcamp
**Chapter:** Day 20 - Intermediate - Build the Snake Game Part 1: Animation & Coordinates
**Lecture:** Create a Snake Class & Move to OOP
**Level:** Intermediate

---

### Summary

So far, we&#x27;ve managed to get our starting segments for our snake to appear on screen. And then we managed to get our snake to automatically move forwards and to figure out a way for the tail of the snake to follow where the head is going. Now it&#x27;s time to tidy up our code a little bit so that all the parts that are related to the snake&#x27;s behavior and the snake&#x27;s appearance go into a separate class. So that by the end of the whole project, we should end up with three classes; a snake class, a food class, and a scoreboard. And all of these classes will be in separate files, managing only one thing. And the goal of the refactoring is so that you could create a separate file, snake.py, import the snake class that you&#x27;ve created in that file. And then once you initialize this snake, it will do everything that we have so far and create our three segments snake onto the screen. And then finally, while the game loop is running, we&#x27;re going to get the snake to continuously move forward just by calling snake .move. This is what you&#x27;re aiming for. Have a think about how you can take the existing code that you&#x27;ve got in the main.py and extract all of the snake related functionality into a separate snake class and make your main.py end up looking like this. The program should do the exact same things as before. The only difference is how the code is organized. Have a think about this. You might need to revise some of the things you learned about how to create classes, how to create objects from the previous lessons. But pause the video, I want you to give this a good try before you come back when we&#x27;ll go through the solution together. Alright. So the first thing we need to do is of course, to create our snake class. And remember that in Python, class names are in Pascal case, so the first letter needs to be capitalized. And then inside this class, I&#x27;m going to create my init. Now the code in here is going to determine what should happen when we initialize a new snake object. So firstly, I&#x27;m going to take the starting positions and take it over to our snake.py. Now, I&#x27;m actually going to put this in as a constant. So right at the top, and remember that in Python, the constants are named with all caps like this, and also with snake case with underscores separating each of the words. Now we can use these starting positions inside our snake class to create our new snake. So we&#x27;re going to need to move our segments over as well. So let&#x27;s create a new attribute that&#x27;s associated with our snake class called segments. And remember, we&#x27;re going to need to use self when we&#x27;re working within a class. After that, we&#x27;re going to create our snake. So I&#x27;m going to create a method called create_snake down here. And this method is going to do everything that we previously had over here, but we&#x27;re going to move that into this method. Now there&#x27;s a couple of things we need to change. Firstly, we change these starting positions into a constant, so let&#x27;s make that consistent. Second thing is that we need to import turtle into this file from the turtle module in order to use it to create a new segment. And then finally, in order to refer to our attribute segments, we have to say self.segments and then append this new segment to our snake segments. That&#x27;s this part done. The next thing we need to do is to get rid of this code and move it inside the snake. So again, I&#x27;m going to create another method that&#x27;s associated with the snake class called move. So when the snake moves, it&#x27;s going to look to this code to figure out how it should do that. Again, because we&#x27;re inside our class, it&#x27;s no longer just segments. It&#x27;s now self.segments. So let&#x27;s go ahead and replace it in all the places where we have our red underlines and now that&#x27;s pretty much it for our snake class. We can create a new snake object and each time we do that, it creates a three-segment snake using the starting positions which are declared at the top. So if we want the snake to show up on screen, then we&#x27;re going to have to first import the snake class from the snake file, and then we&#x27;re going to create a new snake object from that class. And once this line gets triggered, then we&#x27;re going to be calling create_snake and our three-segment snake will show up on screen like this. Now the next step is while the game is on, the screen is going to update every 0.1 second. So that&#x27;s essentially what this is doing. It&#x27;s saying that delay for 0.1 second and then refresh the screen. And every time the screen refreshes, we&#x27;re going to get the snake to move forwards by one step. Now at the moment, each step is defined here as 20. So again, I want to extract that into a constant up here, so we&#x27;re going to call it the MOVE_DISTANCE and we can set that to 20 and then change this to use the constant here. And the reason why we have all these constants is so that if we wanted to tweak our game, say if we wanted the snake to start at a different position or for it to move further each time, then we don&#x27;t have to dig through the body of our code. All we have to do is look at the top, look at all the things that we can change and then change it accordingly. So now coming back to our code here, all we now really have here is just these few lines of code and everything that is snake related, creating the snake or moving the snake is now abstracted into it&#x27;s own class. This way when something goes wrong with our snake, we know who&#x27;s responsible and which file to dig through to figure out the reason. Now, at this point, if we go ahead and rerun our code, you can see that our program works exactly the same as before. Nothing should have changed and nothing should have broken ow because our game isn&#x27;t yet able to go into the game over sequence, there&#x27;s no way of stopping the snake other than hitting the stop button. So don&#x27;t worry if at this stage inside your console you see a lot of red and it says things like keyboard interrupt, or trace back errors. All of these are normal. The thing that you&#x27;re more concerned about is whether if, when you run the game, whether if it actually works like it used to, so namely your snake moving across the screen. If it does exactly what it used to and your main.py is now much simpler and all of your snake functionality and behavior and appearance is now inside its own class, then you have successfully achieved the goal of this lesson.

---

### Key Concepts

| # | Concept | Description |
|---|---------|-------------|
| 1 | **Class definitions (class)** | Introduced/used in this lecture |
| 2 | **while loops** | Introduced/used in this lecture |
| 3 | **if/elif/else conditionals** | Introduced/used in this lecture |
| 4 | **Module imports** | Introduced/used in this lecture |

---

### 🏋️ Practice Exercise

So far, we&#x27;ve managed to get our starting segments for our snake to appear on screen. And then we managed to get our snake to automatically move forwards and to figure out a way for the tail of the snake to follow where the head is going. Now it&#x27;s time to tidy up our code a little bit so that all the parts that are related to the snake&#x27;s behavior and the snake&#x27;s appearance go into a separate class. So that by the end of the whole project, we should end up with three classes; a snake class, a food class, and a scoreboard. And all of these classes will be in separate files, managing only one thing. And the goal of the refactoring is so that you could create a separate file, snake.py, import the snake class that you&#x27;ve created in that file. And then once you initialize this snake, it will do everything that we have so far and create our three segments snake onto the screen. And then finally, while the game loop is running, we&#x27;re going to get the snake to continuously move forward just by calling snake .move. This is what you&#x27;re aiming for. Have a think about how you can take the existing code that you&#x27;ve got in the main.py and extract all of the snake related functionality into a separate snake class and make your main.py end up looking like this. The program should do the exact same things as before. The only difference is how the code is organized. Have a think about this. You might need to revise some of the things you learned about how to create classes, how to create objects from the previous lessons. But pause the video, I want you to give this a good try before you come back when we&#x27;ll go through the solution together. Alright. So the first thing we need to do is of course, to create our snake class. And remember that in Python, class names are in Pascal case, so the first letter needs to be capitalized. And then inside this class, I&#x27;m going to create my init. Now the code in here is going to determine what should happen when we initialize a new snake object. So firstly, I&#x27;m going to take the starting positions and take it over to our snake.py. Now, I&#x27;m actually going to put this in as a constant. So right at the top, and remember that in Python, the constants are named with all caps like this, and also with snake case with underscores separating each of the words. Now we can use these starting positions inside our snake class to create our new snake. So we&#x27;re going to need to move our segments over as well. So let&#x27;s create a new attribute that&#x27;s associated with our snake class called segments. And remember, we&#x27;re going to need to use self when we&#x27;re working within a class. After that, we&#x27;re going to create our snake. So I&#x27;m going to create a method called create_snake down here. And this method is going to do everything that we previously had over here, but we&#x27;re going to move that into this method. Now there&#x27;s a couple of things we need to change. Firstly, we change these starting positions into a constant, so let&#x27;s make that consistent. Second thing is that we need to import turtle into this file from the turtle module in order to use it to create a new segment. And then finally, in order to refer to our attribute segments, we have to say self.segments and then append this new segment to our snake segments. That&#x27;s this part done. The next thing we need to do is to get rid of this code and move it inside the snake. So again, I&#x27;m going to create another method that&#x27;s associated with the snake class called move. So when the snake moves, it&#x27;s going to look to this code to figure out how it should do that. Again, because we&#x27;re inside our class, it&#x27;s no longer just segments. It&#x27;s now self.segments. So let&#x27;s go ahead and replace it in all the places where we have our red underlines and now that&#x27;s pretty much it for our snake class. We can create a new snake object and each time we do that, it creates a three-segment snake using the starting positions which are declared at the top. So if we want the snake to show up on screen, then we&#x27;re going to have to first import the snake class from the snake file, and then we&#x27;re going to create a new snake object from that class. And once this line gets triggered, then we&#x27;re going to be calling create_snake and our three-segment snake will show up on screen like this. Now the next step is while the game is on, the screen is going to update every 0.1 second. So that&#x27;s essentially what this is doing. It&#x27;s saying that delay for 0.1 second and then refresh the screen. And every time the screen refreshes, we&#x27;re going to get the snake to move forwards by one step. Now at the moment, each step is defined here as 20. So again, I want to extract that into a constant up here, so we&#x27;re going to call it the MOVE_DISTANCE and we can set that to 20 and then change this to use the constant here. And the reason why we have all these constants is so that if we wanted to tweak our game, say if we wanted the snake to start at a different position or for it to move further each time, then we don&#x27;t have to dig through the body of our code. All we have to do is look at the top, look at all the things that we can change and then change it accordingly. So now coming back to our code here, all we now really have here is just these few lines of code and everything that is snake related, creating the snake or moving the snake is now abstracted into it&#x27;s own class. This way when something goes wrong with our snake, we know who&#x27;s responsible and which file to dig through to figure out the reason. Now, at this point, if we go ahead and rerun our code, you can see that our program works exactly the same as before. Nothing should have changed and nothing should have broken ow because our game isn&#x27;t yet able to go into the game over sequence, there&#x27;s no way of stopping the snake other than hitting the stop button. So don&#x27;t worry if at this stage inside your console you see a lot of red and it says things like keyboard interrupt, or trace back errors. All of these are normal. The thing that you&#x27;re more concerned about is whether if, when you run the game, whether if it actually works like it used to, so namely your snake moving across the screen. If it does exactly what it used to and your main.py is now much simpler and all of your snake functionality and behavior and appearance is now inside its own class, then you have successfully achieved the goal of this lesson.

---
