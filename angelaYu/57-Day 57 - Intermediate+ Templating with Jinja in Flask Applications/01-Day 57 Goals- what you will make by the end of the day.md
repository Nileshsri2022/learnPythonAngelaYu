Here is a structured breakdown of this lesson on the goals for Day 57.

---

### 1. Jinja: Python Inside HTML

Templates with **placeholders** that Flask fills at request time:

* `{{ variable }}` — print a value.
* `{% if %}…{% endif %}` / `{% for %}…{% endfor %}` — logic and loops.
* `{{ url_for('route_name') }}` — links that never go stale.

---

### 2. Today's Arc

1. Dynamic pages with Jinja variables.
2. Challenge: feed templates with **API data** (agify + genderize).
3. Multiline Jinja: loops and conditionals.
4. URL building with `url_for`.
5. Project: **Blog Capstone Part 1** — the blog's pages and templating.

---

### Summary Checklist

1. Server computes; template displays.
2. The blog capstone starts here and runs through Day 59.
