# 📖 Rendering HTML Files with Flask

---

### Overview

**Course:** 100 Days of Code™: The Complete Python Pro Bootcamp
**Chapter:** Day 56 - Intermediate+ Rendering HTML/Static files and Using Website Templates
**Lecture:** Rendering HTML Files with Flask
**Level:** Intermediate+ Rendering HTML

---

### Summary

Now the first thing I want to show you is how to render HTML files. Go ahead and create a new PyCharm project. I&#x27;ve called mine &quot;my-personal-site&quot;, but you can call yours anything you like. And, in the project, we&#x27;re going to create a new file, which is going to be our Python server. So I&#x27;ll call it &quot;server.py&quot;. And then inside our server.py, we&#x27;re going to go through all the stages of building up a simple Flask server. This might be a good time to revise how to do that. So, go ahead, pause the video and see if you can set up a simple Flask application which just serves up &quot;Hello World&quot; to the homepage. All right, so let&#x27;s start from the beginning. We&#x27;ll import the Flask class from the flask application and as usual, I&#x27;m going to use my red light bulb to install the Flask package. Now, once I&#x27;ve done that, I&#x27;m going to create my Flask application by saying, app = Flask, and then inside the initialization for my Flask application, I&#x27;m going to put in the (__name__) of my current directory. Next, we&#x27;re going to use the route decorator to target the home route. And when the user hits up the home route, we&#x27;re going to render the home page. So, inside this definition, we&#x27;re simply just going to return &quot;Hello World&quot;. And finally, we&#x27;re ready to go ahead and check to see if the __name__ is equal to &quot;__main__&quot;, then, in that case, we&#x27;re going to run our app. That&#x27;s it. That&#x27;s all there is to it. Let&#x27;s go ahead and run this application, which is our server.py. And if we head over to this URL, we can see &quot;Hello World&quot; when we go to our homepage. The next step is to figure out how can we render an HTML webpage, which is crafted inside an HTML file, rather than simply just returning strings or small bits of HTML elements? Let&#x27;s go into our file and go ahead and create a new HTML file. Now, the reason why we&#x27;re creating it like this is because it&#x27;s going to add in all of the boilerplate code that is required to an HTML website. So, let&#x27;s go ahead and call our webpage &quot;index.html&quot;. And you can see, as soon as I create it, it&#x27;s added in the DOCTYPE, it&#x27;s added in the html, the head, and the body. Now, if this is unfamiliar to you, be sure to check out the lessons where we covered HTML and CSS in detail, but I&#x27;m going to assume that all of this is pretty straightforward by now. Now, the first thing we&#x27;re going to change in our new website is the title. So, I&#x27;m going to go ahead and give this a title. You can put anything you want, but I&#x27;m just going to put my own name here. Now, inside the body, I&#x27;m going to create a h1 and you can see that the HTML files on PyCharm has that tag completion that we see here. In my h1, I&#x27;m just going to write &quot;I&#x27;m Angela&quot; (chuckles). Pretty straightforward. This is a complete HTML file, now. Now, if I want to render this instead of just &quot;Hello World&quot;, how would I do that? Well, how do we find out? As always, it&#x27;s to the documentation. In the documentation of the Quickstart for Flask, there&#x27;s a section on Rendering Templates and this allows us to render an HTML file as a template. There&#x27;s a couple of requirements though. The first thing is that remember that Flask is a framework and not a library, so it will put certain restrictions and requirements on your code so that it will actually work. And in this case, our HTML files, which is known as our templates, must be inside a folder called &quot;templates&quot;. That&#x27;s where Flask is going to look for it when we try to render it. So, let&#x27;s first do that. Let&#x27;s create a new directory, which we&#x27;ll call &quot;templates&quot;, all lowercase. And let&#x27;s move our index.html into that new folder. So, there it is. The next thing we have to do is we have to import this method called &quot;render_template&quot; from the Flask module. So, let&#x27;s go back to our server.py. And in addition to importing the Flask class, let&#x27;s go ahead and import the render_template method as well. The next thing we have to do is to, instead of returning a string or a string that contains HTML, we have to call this method &quot;render_template&quot; and pass in the file name that we have and then that is going to be rendered into our website. So, let&#x27;s go ahead and do this. Let&#x27;s delete that &quot;Hello World&quot; and instead, let&#x27;s return the output from this render_template method. And inside here, we&#x27;re going to put the name of our HTML file, which lives inside our templates folder. That&#x27;s going to be &quot;index.html&quot; because that&#x27;s what we called it. Now, let&#x27;s go ahead and run our code. Alternatively, let&#x27;s switch on the debug mode so that it will refresh and rerun our server every time we make a change. So, let&#x27;s rerun and now let&#x27;s go to our website, refresh the main page, and you can see it&#x27;s now got an h1 in here. And in the head, there is now the title, which is Angela, which is the title of the tab as well. Imagine if you had to type all of this HTML into this return as a string. It&#x27;s pretty painful. It doesn&#x27;t have any of the assistance that you get from your editor. Now, here&#x27;s a challenge for you. Back on the days 41 and 42, we learned about HTML and created some simple websites. Now, what I want you to do is either use one of your own HTML websites or download my very basic CV website from this lessons resources. And then go to the link either on your GitHub page or find the original HTML file and I want you to go ahead and get hold of it. So, if it&#x27;s on GitHub, then you can go ahead and simply save as and save this webpage into your downloads. Now, if it&#x27;s saved as an HTML file somewhere on your system, that&#x27;s also fine. But essentially, what we want to do is we want to take this file, which is an HTML file, and we want to move it into our templates. Now, one of the things that you&#x27;ll notice is if you downloaded this file off the internet, then you might have to rename it, because firstly, it&#x27;s really long and we don&#x27;t want to have to type that into our code. So, I&#x27;m going to shorten the name to &quot;Angela&quot;. And then, at the very end, you can see the file extension, at least when it comes off GitHub, is dot htm, and the reason for this is because some servers don&#x27;t accept four-lettered file extensions. But it&#x27;s exactly the same as .html. So, we&#x27;re going to change it to .html and then we&#x27;re going to refactor that. So, now that we&#x27;ve got hold of this HTML page, we&#x27;re going to try and render it instead of the index.html. Pause the video and see if you can complete this as a challenge. All right, that&#x27;s pretty simple. All we have to do is just replace the &quot;index.html&quot; to the new file, which is &quot;angela.html&quot;, and if that file happened to be inside a folder, for example, if we had a sub folder inside templates, which we called, I don&#x27;t know, &quot;cv&quot;, and we had this inside here, then we would have to specify that in the file path. If we just have it in the top level of our templates folder, then it&#x27;s as simple as what we did with the &quot;index.html&quot;. So now, while our server is running in debug mode, we can go ahead and hit &quot;Save&quot; on this website and it should restart it, our server. And now, if we go back to our main homepage and reload the website, you can see it&#x27;s now rendering our personal CV site. With only one exception, what has happened to this profile picture? It looks pretty broken. Now, in order to be able to render static images, we&#x27;re going to have to learn how to render static files, such as images like this. That&#x27;s what we&#x27;re going do in the next lesson.

---

### Key Concepts

| # | Concept | Description |
|---|---------|-------------|
| 1 | **Class definitions (class)** | Introduced/used in this lecture |
| 2 | **while loops** | Introduced/used in this lecture |
| 3 | **if/elif/else conditionals** | Introduced/used in this lecture |
| 4 | **Module imports** | Introduced/used in this lecture |
| 5 | **Flask web framework** | Introduced/used in this lecture |

---

### 🏋️ Practice Exercise

Now the first thing I want to show you is how to render HTML files. Go ahead and create a new PyCharm project. I&#x27;ve called mine &quot;my-personal-site&quot;, but you can call yours anything you like. And, in the project, we&#x27;re going to create a new file, which is going to be our Python server. So I&#x27;ll call it &quot;server.py&quot;. And then inside our server.py, we&#x27;re going to go through all the stages of building up a simple Flask server. This might be a good time to revise how to do that. So, go ahead, pause the video and see if you can set up a simple Flask application which just serves up &quot;Hello World&quot; to the homepage. All right, so let&#x27;s start from the beginning. We&#x27;ll import the Flask class from the flask application and as usual, I&#x27;m going to use my red light bulb to install the Flask package. Now, once I&#x27;ve done that, I&#x27;m going to create my Flask application by saying, app = Flask, and then inside the initialization for my Flask application, I&#x27;m going to put in the (__name__) of my current directory. Next, we&#x27;re going to use the route decorator to target the home route. And when the user hits up the home route, we&#x27;re going to render the home page. So, inside this definition, we&#x27;re simply just going to return &quot;Hello World&quot;. And finally, we&#x27;re ready to go ahead and check to see if the __name__ is equal to &quot;__main__&quot;, then, in that case, we&#x27;re going to run our app. That&#x27;s it. That&#x27;s all there is to it. Let&#x27;s go ahead and run this application, which is our server.py. And if we head over to this URL, we can see &quot;Hello World&quot; when we go to our homepage. The next step is to figure out how can we render an HTML webpage, which is crafted inside an HTML file, rather than simply just returning strings or small bits of HTML elements? Let&#x27;s go into our file and go ahead and create a new HTML file. Now, the reason why we&#x27;re creating it like this is because it&#x27;s going to add in all of the boilerplate code that is required to an HTML website. So, let&#x27;s go ahead and call our webpage &quot;index.html&quot;. And you can see, as soon as I create it, it&#x27;s added in the DOCTYPE, it&#x27;s added in the html, the head, and the body. Now, if this is unfamiliar to you, be sure to check out the lessons where we covered HTML and CSS in detail, but I&#x27;m going to assume that all of this is pretty straightforward by now. Now, the first thing we&#x27;re going to change in our new website is the title. So, I&#x27;m going to go ahead and give this a title. You can put anything you want, but I&#x27;m just going to put my own name here. Now, inside the body, I&#x27;m going to create a h1 and you can see that the HTML files on PyCharm has that tag completion that we see here. In my h1, I&#x27;m just going to write &quot;I&#x27;m Angela&quot; (chuckles). Pretty straightforward. This is a complete HTML file, now. Now, if I want to render this instead of just &quot;Hello World&quot;, how would I do that? Well, how do we find out? As always, it&#x27;s to the documentation. In the documentation of the Quickstart for Flask, there&#x27;s a section on Rendering Templates and this allows us to render an HTML file as a template. There&#x27;s a couple of requirements though. The first thing is that remember that Flask is a framework and not a library, so it will put certain restrictions and requirements on your code so that it will actually work. And in this case, our HTML files, which is known as our templates, must be inside a folder called &quot;templates&quot;. That&#x27;s where Flask is going to look for it when we try to render it. So, let&#x27;s first do that. Let&#x27;s create a new directory, which we&#x27;ll call &quot;templates&quot;, all lowercase. And let&#x27;s move our index.html into that new folder. So, there it is. The next thing we have to do is we have to import this method called &quot;render_template&quot; from the Flask module. So, let&#x27;s go back to our server.py. And in addition to importing the Flask class, let&#x27;s go ahead and import the render_template method as well. The next thing we have to do is to, instead of returning a string or a string that contains HTML, we have to call this method &quot;render_template&quot; and pass in the file name that we have and then that is going to be rendered into our website. So, let&#x27;s go ahead and do this. Let&#x27;s delete that &quot;Hello World&quot; and instead, let&#x27;s return the output from this render_template method. And inside here, we&#x27;re going to put the name of our HTML file, which lives inside our templates folder. That&#x27;s going to be &quot;index.html&quot; because that&#x27;s what we called it. Now, let&#x27;s go ahead and run our code. Alternatively, let&#x27;s switch on the debug mode so that it will refresh and rerun our server every time we make a change. So, let&#x27;s rerun and now let&#x27;s go to our website, refresh the main page, and you can see it&#x27;s now got an h1 in here. And in the head, there is now the title, which is Angela, which is the title of the tab as well. Imagine if you had to type all of this HTML into this return as a string. It&#x27;s pretty painful. It doesn&#x27;t have any of the assistance that you get from your editor. Now, here&#x27;s a challenge for you. Back on the days 41 and 42, we learned about HTML and created some simple websites. Now, what I want you to do is either use one of your own HTML websites or download my very basic CV website from this lessons resources. And then go to the link either on your GitHub page or find the original HTML file and I want you to go ahead and get hold of it. So, if it&#x27;s on GitHub, then you can go ahead and simply save as and save this webpage into your downloads. Now, if it&#x27;s saved as an HTML file somewhere on your system, that&#x27;s also fine. But essentially, what we want to do is we want to take this file, which is an HTML file, and we want to move it into our templates. Now, one of the things that you&#x27;ll notice is if you downloaded this file off the internet, then you might have to rename it, because firstly, it&#x27;s really long and we don&#x27;t want to have to type that into our code. So, I&#x27;m going to shorten the name to &quot;Angela&quot;. And then, at the very end, you can see the file extension, at least when it comes off GitHub, is dot htm, and the reason for this is because some servers don&#x27;t accept four-lettered file extensions. But it&#x27;s exactly the same as .html. So, we&#x27;re going to change it to .html and then we&#x27;re going to refactor that. So, now that we&#x27;ve got hold of this HTML page, we&#x27;re going to try and render it instead of the index.html. Pause the video and see if you can complete this as a challenge. All right, that&#x27;s pretty simple. All we have to do is just replace the &quot;index.html&quot; to the new file, which is &quot;angela.html&quot;, and if that file happened to be inside a folder, for example, if we had a sub folder inside templates, which we called, I don&#x27;t know, &quot;cv&quot;, and we had this inside here, then we would have to specify that in the file path. If we just have it in the top level of our templates folder, then it&#x27;s as simple as what we did with the &quot;index.html&quot;. So now, while our server is running in debug mode, we can go ahead and hit &quot;Save&quot; on this website and it should restart it, our server. And now, if we go back to our main homepage and reload the website, you can see it&#x27;s now rendering our personal CV site. With only one exception, what has happened to this profile picture? It looks pretty broken. Now, in order to be able to render static images, we&#x27;re going to have to learn how to render static files, such as images like this. That&#x27;s what we&#x27;re going do in the next lesson.

---

### Next Steps

Now the first thing I want to show you is how to render HTML files. Go ahead and create a new PyCharm project. I&#x27;ve called mine &quot;my-personal-site&quot;, but you can call yours anything you like. And, in the project, we&#x27;re going to create a new file, which is going to be our Python server. So I&#x27;ll call it &quot;server.py&quot;. And then inside our server.py, we&#x27;re going to go through all the stages of building up a simple Flask server. This might be a good time to revise how to do that. So, go ahead, pause the video and see if you can set up a simple Flask application which just serves up &quot;Hello World&quot; to the homepage. All right, so let&#x27;s start from the beginning. We&#x27;ll import the Flask class from the flask application and as usual, I&#x27;m going to use my red light bulb to install the Flask package. Now, once I&#x27;ve done that, I&#x27;m going to create my Flask application by saying, app = Flask, and then inside the initialization for my Flask application, I&#x27;m going to put in the (__name__) of my current directory. Next, we&#x27;re going to use the route decorator to target the home route. And when the user hits up the home route, we&#x27;re going to render the home page. So, inside this definition, we&#x27;re simply just going to return &quot;Hello World&quot;. And finally, we&#x27;re ready to go ahead and check to see if the __name__ is equal to &quot;__main__&quot;, then, in that case, we&#x27;re going to run our app. That&#x27;s it. That&#x27;s all there is to it. Let&#x27;s go ahead and run this application, which is our server.py. And if we head over to this URL, we can see &quot;Hello World&quot; when we go to our homepage. The next step is to figure out how can we render an HTML webpage, which is crafted inside an HTML file, rather than simply just returning strings or small bits of HTML elements? Let&#x27;s go into our file and go ahead and create a new HTML file. Now, the reason why we&#x27;re creating it like this is because it&#x27;s going to add in all of the boilerplate code that is required to an HTML website. So, let&#x27;s go ahead and call our webpage &quot;index.html&quot;. And you can see, as soon as I create it, it&#x27;s added in the DOCTYPE, it&#x27;s added in the html, the head, and the body. Now, if this is unfamiliar to you, be sure to check out the lessons where we covered HTML and CSS in detail, but I&#x27;m going to assume that all of this is pretty straightforward by now. Now, the first thing we&#x27;re going to change in our new website is the title. So, I&#x27;m going to go ahead and give this a title. You can put anything you want, but I&#x27;m just going to put my own name here. Now, inside the body, I&#x27;m going to create a h1 and you can see that the HTML files on PyCharm has that tag completion that we see here. In my h1, I&#x27;m just going to write &quot;I&#x27;m Angela&quot; (chuckles). Pretty straightforward. This is a complete HTML file, now. Now, if I want to render this instead of just &quot;Hello World&quot;, how would I do that? Well, how do we find out? As always, it&#x27;s to the documentation. In the documentation of the Quickstart for Flask, there&#x27;s a section on Rendering Templates and this allows us to render an HTML file as a template. There&#x27;s a couple of requirements though. The first thing is that remember that Flask is a framework and not a library, so it will put certain restrictions and requirements on your code so that it will actually work. And in this case, our HTML files, which is known as our templates, must be inside a folder called &quot;templates&quot;. That&#x27;s where Flask is going to look for it when we try to render it. So, let&#x27;s first do that. Let&#x27;s create a new directory, which we&#x27;ll call &quot;templates&quot;, all lowercase. And let&#x27;s move our index.html into that new folder. So, there it is. The next thing we have to do is we have to import this method called &quot;render_template&quot; from the Flask module. So, let&#x27;s go back to our server.py. And in addition to importing the Flask class, let&#x27;s go ahead and import the render_template method as well. The next thing we have to do is to, instead of returning a string or a string that contains HTML, we have to call this method &quot;render_template&quot; and pass in the file name that we have and then that is going to be rendered into our website. So, let&#x27;s go ahead and do this. Let&#x27;s delete that &quot;Hello World&quot; and instead, let&#x27;s return the output from this render_template method. And inside here, we&#x27;re going to put the name of our HTML file, which lives inside our templates folder. That&#x27;s going to be &quot;index.html&quot; because that&#x27;s what we called it. Now, let&#x27;s go ahead and run our code. Alternatively, let&#x27;s switch on the debug mode so that it will refresh and rerun our server every time we make a change. So, let&#x27;s rerun and now let&#x27;s go to our website, refresh the main page, and you can see it&#x27;s now got an h1 in here. And in the head, there is now the title, which is Angela, which is the title of the tab as well. Imagine if you had to type all of this HTML into this return as a string. It&#x27;s pretty painful. It doesn&#x27;t have any of the assistance that you get from your editor. Now, here&#x27;s a challenge for you. Back on the days 41 and 42, we learned about HTML and created some simple websites. Now, what I want you to do is either use one of your own HTML websites or download my very basic CV website from this lessons resources. And then go to the link either on your GitHub page or find the original HTML file and I want you to go ahead and get hold of it. So, if it&#x27;s on GitHub, then you can go ahead and simply save as and save this webpage into your downloads. Now, if it&#x27;s saved as an HTML file somewhere on your system, that&#x27;s also fine. But essentially, what we want to do is we want to take this file, which is an HTML file, and we want to move it into our templates. Now, one of the things that you&#x27;ll notice is if you downloaded this file off the internet, then you might have to rename it, because firstly, it&#x27;s really long and we don&#x27;t want to have to type that into our code. So, I&#x27;m going to shorten the name to &quot;Angela&quot;. And then, at the very end, you can see the file extension, at least when it comes off GitHub, is dot htm, and the reason for this is because some servers don&#x27;t accept four-lettered file extensions. But it&#x27;s exactly the same as .html. So, we&#x27;re going to change it to .html and then we&#x27;re going to refactor that. So, now that we&#x27;ve got hold of this HTML page, we&#x27;re going to try and render it instead of the index.html. Pause the video and see if you can complete this as a challenge. All right, that&#x27;s pretty simple. All we have to do is just replace the &quot;index.html&quot; to the new file, which is &quot;angela.html&quot;, and if that file happened to be inside a folder, for example, if we had a sub folder inside templates, which we called, I don&#x27;t know, &quot;cv&quot;, and we had this inside here, then we would have to specify that in the file path. If we just have it in the top level of our templates folder, then it&#x27;s as simple as what we did with the &quot;index.html&quot;. So now, while our server is running in debug mode, we can go ahead and hit &quot;Save&quot; on this website and it should restart it, our server. And now, if we go back to our main homepage and reload the website, you can see it&#x27;s now rendering our personal CV site. With only one exception, what has happened to this profile picture? It looks pretty broken. Now, in order to be able to render static images, we&#x27;re going to have to learn how to render static files, such as images like this. That&#x27;s what we&#x27;re going do in the next lesson.
