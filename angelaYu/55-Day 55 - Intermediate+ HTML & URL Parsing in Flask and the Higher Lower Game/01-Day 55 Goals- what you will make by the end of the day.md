Here is a structured breakdown of this lesson on the goals for Day 55.

---

### 1. Today's Menu

1. **URL paths** — let users type extra path info (`/angela`, `/mail/messages`) and
   read it in Python.
2. **HTML from Flask** — return real markup, not just text.
3. **Decorators round two** — style HTML with your own decorators, then level up with
   `*args`/`**kwargs`.
4. **Project** — Higher or Lower, played through the URL bar.

---

### Summary Checklist

1. Path variables = user input without forms.
2. Your decorators now wrap functions *with arguments*.
