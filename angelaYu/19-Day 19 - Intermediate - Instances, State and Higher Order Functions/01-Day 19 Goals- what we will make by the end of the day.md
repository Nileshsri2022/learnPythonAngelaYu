Here is a structured breakdown of the Day 19 goals — the start of the Snake Game.

---

### 1. Skills Covered on Day 19

* **Higher-order functions** — functions that accept other functions
* **Event listeners** — wiring keyboard input to function calls
* **Object state** — each instance keeps its own data
* **Instances** — many objects from one class, behaving independently

---

### 2. Today's Build (Snake Part 1 of 3)

* An **Etch-A-Sketch** mini-app: arrow keys draw on screen (event listeners in action).
* The **turtle race game**: several turtle instances racing by their own random state.
* These mechanics — objects, state, input events — are the foundations of the Snake
  game starting on Day 20.

---

### Summary Checklist

1. GUI programs = objects + state + event callbacks.
2. Higher-order functions pass behaviour around like data.
3. Race turtles today; animate a snake tomorrow.
