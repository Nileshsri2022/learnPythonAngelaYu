# 🔧 Python Higher Order Functions & Event Listeners

---

### Overview

**Course:** 100 Days of Code™: The Complete Python Pro Bootcamp
**Chapter:** Day 19 - Intermediate - Instances, State and Higher Order Functions
**Lecture:** Python Higher Order Functions & Event Listeners
**Level:** Intermediate

---

### Summary

Now in order to be able to create a lot of the games that we&#x27;ve spoken about, we need a way of being able to listen to things the user does, like when the user taps a specific key on the keyboard. And the code that allows us to do this are called event listeners. So if we look at the turtle documentation, you can see that there&#x27;s a whole section on screen events, including listening to key presses or listening to a click or other things that you can listen to. So the important thing is this listen method. This allows the turtle screen to start listening and waiting for events that the user might trigger, like tapping on a key. I&#x27;ve gone ahead and created a new PyCharm project which I&#x27;ve called day-19 and I&#x27;ve created my main.py file inside the project. Now, the first thing I&#x27;m going to do is I&#x27;m going to import the turtle class and the screen class from the turtle module. And then I&#x27;m going to use them to create my Tim the turtle, and also create a screen object. This is basically going to control the window when we run our code. So you saw it just flash just now. Now in order to start listening for events, we have to get hold of the screen object and then tell it to start listening. And once it starts listening, we have to bind a function that will be triggered when a particular key is pressed on the keyboard. Now, in order to bind a key stroke to an event in our code, we have to use an event listener. So the one that we&#x27;re going to be using is this onkey method. And you can see it expects a function with no arguments and also a key, like the &#x27;a&#x27; key on your keyboard or a key symbol, so the space or up or down. So I&#x27;m going to say screen.onkey and then I have to bind some sort of function to this method. So let&#x27;s create a function and we&#x27;ll call it move_ forwards. And all that this function is going to do is it&#x27;s going to get Tim to go forward by 10 paces. And let&#x27;s not forget our colon here. Now, coming back to my onkey method, I&#x27;m going to say that when the space key, so the space bar, is pressed, then trigger the function move_forwards. Now here&#x27;s a really important point; when we use a function as a argument, so something that is going to be passed into another function, we don&#x27;t actually add the parentheses at the end. The parentheses triggers the function to happen there and then. But what we want is we want this method, onkey, to listen for when the space bar is pressed and only when that happens to trigger this move_forwards function. Now, the final thing I need to do is to add the exitonclick so that my screen doesn&#x27;t disappear when I run it. And now what I&#x27;m going to try is when I hit the spacebar, you should see my turtle move forwards by 10 paces each time. So we&#x27;re now controlling our turtle using a keystroke. What we&#x27;ve essentially got here is a function that&#x27;s being used as an input. Let&#x27;s say we have a function called function_a that takes some sort of input. And then we have a function called function_b, we can actually pass function_b into function_a like this. So notice that when we pass a function as an input, we only pass the name without the parentheses at the end. But how exactly does this function onkey work? Because it&#x27;s kind of crazy to use a function as the input to another function, right? So want to show you a quick example of passing a function into another function, and I&#x27;m going to use the calculator example that we did many, many moons ago. Um, when we created a calculator that could take two numbers, add, subtract, multiply, or divide those numbers. So we&#x27;ve got our four functions here. Now, you don&#x27;t actually have to write any of this out, cause it&#x27;s quite tedious. I just want you to watch and listen. So let&#x27;s say I create a final function, which I&#x27;ll call calculator. And this function is going to take a n1 and n2, two numbers, but it&#x27;s also going to take a function as an input. Now, inside this calculator function, I&#x27;m going to take the function name that was passed in and then call that function by adding the parentheses and then passing in n1 and n2. So now I have my calculator function which can take any function as an input. And once that function has been passed in here, then that function will get triggered passing in n1 and n2. Let&#x27;s return this result as the output, and then let&#x27;s call our calculator function, let&#x27;s pass in some numbers and let&#x27;s pass in the name of a function. So now let&#x27;s go ahead and save this into a variable and then print that variable result. So now when I run the code, you can see I get five printed and that&#x27;s because I&#x27;ve got add as the third input here. But what happens if I change this to multiply? Well, I get six instead of five. And what happens if I put divide? Well, I get zero because, because it&#x27;s rounding up to a whole number. But essentially what I&#x27;m trying to demonstrate to you is this concept which is known in Python as Higher Order Functions. And the idea of a higher order function is a function that can work with other functions. So in this case, our calculator is a higher order function because it&#x27;s actually taking another function as an input, and then working with it inside the body of the function. Now there are certain programming languages where you can&#x27;t do this at all. But in Python, this is pretty commonly used. And it&#x27;s really useful when we need to listen for events and then trigger a particular function. Now, while we&#x27;re on the topic of functions, I recommend that when you&#x27;re using methods that you haven&#x27;t created yourself like for example onkey, to use keyword arguments instead of positional arguments especially if like in this case, the position doesn&#x27;t actually have any meaning. So now that you&#x27;ve seen how event listeners work, we&#x27;re going to be using event listeners to build out our Etch-A-Sketch program in the next lesson. So for all of that and more, I&#x27;ll see you there.

---

### Key Concepts

| # | Concept | Description |
|---|---------|-------------|
| 1 | **Class definitions (class)** | Introduced/used in this lecture |
| 2 | **while loops** | Introduced/used in this lecture |
| 3 | **if/elif/else conditionals** | Introduced/used in this lecture |
| 4 | **Module imports** | Introduced/used in this lecture |

---

### Next Steps

Now in order to be able to create a lot of the games that we&#x27;ve spoken about, we need a way of being able to listen to things the user does, like when the user taps a specific key on the keyboard. And the code that allows us to do this are called event listeners. So if we look at the turtle documentation, you can see that there&#x27;s a whole section on screen events, including listening to key presses or listening to a click or other things that you can listen to. So the important thing is this listen method. This allows the turtle screen to start listening and waiting for events that the user might trigger, like tapping on a key. I&#x27;ve gone ahead and created a new PyCharm project which I&#x27;ve called day-19 and I&#x27;ve created my main.py file inside the project. Now, the first thing I&#x27;m going to do is I&#x27;m going to import the turtle class and the screen class from the turtle module. And then I&#x27;m going to use them to create my Tim the turtle, and also create a screen object. This is basically going to control the window when we run our code. So you saw it just flash just now. Now in order to start listening for events, we have to get hold of the screen object and then tell it to start listening. And once it starts listening, we have to bind a function that will be triggered when a particular key is pressed on the keyboard. Now, in order to bind a key stroke to an event in our code, we have to use an event listener. So the one that we&#x27;re going to be using is this onkey method. And you can see it expects a function with no arguments and also a key, like the &#x27;a&#x27; key on your keyboard or a key symbol, so the space or up or down. So I&#x27;m going to say screen.onkey and then I have to bind some sort of function to this method. So let&#x27;s create a function and we&#x27;ll call it move_ forwards. And all that this function is going to do is it&#x27;s going to get Tim to go forward by 10 paces. And let&#x27;s not forget our colon here. Now, coming back to my onkey method, I&#x27;m going to say that when the space key, so the space bar, is pressed, then trigger the function move_forwards. Now here&#x27;s a really important point; when we use a function as a argument, so something that is going to be passed into another function, we don&#x27;t actually add the parentheses at the end. The parentheses triggers the function to happen there and then. But what we want is we want this method, onkey, to listen for when the space bar is pressed and only when that happens to trigger this move_forwards function. Now, the final thing I need to do is to add the exitonclick so that my screen doesn&#x27;t disappear when I run it. And now what I&#x27;m going to try is when I hit the spacebar, you should see my turtle move forwards by 10 paces each time. So we&#x27;re now controlling our turtle using a keystroke. What we&#x27;ve essentially got here is a function that&#x27;s being used as an input. Let&#x27;s say we have a function called function_a that takes some sort of input. And then we have a function called function_b, we can actually pass function_b into function_a like this. So notice that when we pass a function as an input, we only pass the name without the parentheses at the end. But how exactly does this function onkey work? Because it&#x27;s kind of crazy to use a function as the input to another function, right? So want to show you a quick example of passing a function into another function, and I&#x27;m going to use the calculator example that we did many, many moons ago. Um, when we created a calculator that could take two numbers, add, subtract, multiply, or divide those numbers. So we&#x27;ve got our four functions here. Now, you don&#x27;t actually have to write any of this out, cause it&#x27;s quite tedious. I just want you to watch and listen. So let&#x27;s say I create a final function, which I&#x27;ll call calculator. And this function is going to take a n1 and n2, two numbers, but it&#x27;s also going to take a function as an input. Now, inside this calculator function, I&#x27;m going to take the function name that was passed in and then call that function by adding the parentheses and then passing in n1 and n2. So now I have my calculator function which can take any function as an input. And once that function has been passed in here, then that function will get triggered passing in n1 and n2. Let&#x27;s return this result as the output, and then let&#x27;s call our calculator function, let&#x27;s pass in some numbers and let&#x27;s pass in the name of a function. So now let&#x27;s go ahead and save this into a variable and then print that variable result. So now when I run the code, you can see I get five printed and that&#x27;s because I&#x27;ve got add as the third input here. But what happens if I change this to multiply? Well, I get six instead of five. And what happens if I put divide? Well, I get zero because, because it&#x27;s rounding up to a whole number. But essentially what I&#x27;m trying to demonstrate to you is this concept which is known in Python as Higher Order Functions. And the idea of a higher order function is a function that can work with other functions. So in this case, our calculator is a higher order function because it&#x27;s actually taking another function as an input, and then working with it inside the body of the function. Now there are certain programming languages where you can&#x27;t do this at all. But in Python, this is pretty commonly used. And it&#x27;s really useful when we need to listen for events and then trigger a particular function. Now, while we&#x27;re on the topic of functions, I recommend that when you&#x27;re using methods that you haven&#x27;t created yourself like for example onkey, to use keyword arguments instead of positional arguments especially if like in this case, the position doesn&#x27;t actually have any meaning. So now that you&#x27;ve seen how event listeners work, we&#x27;re going to be using event listeners to build out our Etch-A-Sketch program in the next lesson. So for all of that and more, I&#x27;ll see you there.
