# 🔧 Write, read and update JSON data in the Password Manager

---

### Overview

**Course:** 100 Days of Code™: The Complete Python Pro Bootcamp
**Chapter:** Day 30 - Intermediate - Errors, Exceptions and JSON Data: Improving the Password
**Lecture:** Write, read and update JSON data in the Password Manager
**Level:** Intermediate

---

### Summary

The goal is to take our password manager to the next level. And in order to do that, we need to add a search functionality. It&#x27;s not good enough just to look through the data file anymore. What we want to be able to do is to type a website into the website entry, and then hit search, and we get a pop up showing us our email and the password that was saved for that website. So this is the goal. But where we&#x27;re currently at is we have a text file which contains the name of the website, the email and the password. This is very difficult to search and this is a really terrible format to work with. So we&#x27;re going to be levelling up our data storage in our password manager. And we&#x27;re going to switch from saving data straight to a text file to a fancier data format, which is called JSON. Nope. This is not the latest, hottest DJ on the block. Lock. It&#x27;s actually JSON, which stands for JavaScript Object Notation. This was something that was designed originally for JavaScript, but because it has such a simple structure and it&#x27;s so easy to understand and work with that it&#x27;s been adopted by many, many different fields, including Python. This is probably one of the most popular ways of transferring data, especially when you&#x27;re transferring data across the internet. So this is what we&#x27;re going to be learning about. And for those of you looking at this slide, you might have already realized that it&#x27;s kind of similar to the dictionaries that we&#x27;ve been working with in Python. Right? A JSON is essentially composed of a bunch of nested lists and dictionaries, and it has that key value pair data structure. This is essentially what we&#x27;re aiming for, so that we can store our data in this format. And we&#x27;ll be able to easily load up this data and search through it for the particular website that we want the information for. To work with JSON data in Python, we can use the inbuilt JSON library, and we&#x27;re going to use it to write, read and update data to a JSON file. To begin, I&#x27;m going to simplify our code a little bit. I&#x27;m going to get rid of this message box which asks the user okay or cancel. We&#x27;re just going to assume that once they click add, then they&#x27;re happy with the email and password. So now that we&#x27;ve cut down that little bit of code, it makes this a little bit easier to understand. All that we&#x27;re doing here is we&#x27;re checking to make sure that the length of the website entry and the length of the password entry is equal to zero. And you might still have the length checking for the email entry. But remember that at the end of previous lessons, we actually inserted a default value so we can actually skip that check as well if you wanted to. The reason why we want all code as simple as possible is so that when we&#x27;re working with the JSON and we&#x27;re learning about how to work with it, as long as everything else is simple, then we can focus on the new things that we&#x27;re learning. In this case, instead of opening a data dot txt file, I&#x27;m going to change the data format to a dot JSON and instead of using the append mode, I&#x27;m going to use the write mode because I&#x27;m going to be writing to this data dot JSON file. Now, in order to write to a JSON file, we don&#x27;t use this line of code. Instead we use a method called JSON dot dump. And that of course requires us to import the JSON module, which should be inbuilt to Python so you don&#x27;t actually have to install it. Now, once we&#x27;ve imported our JSON module we can say JSON dot dump. And this takes a number of inputs. But the most important are the things that you want to dump and the file that you want to dump it to. The data that we want to put in here should go in as a dictionary. Essentially, what we want to create is a new dictionary, which I&#x27;ll call new data. And it&#x27;s going to be a nested dictionary. So the first level key is going to be the website, because this is what we&#x27;re going to be searching through eventually. And the website is going to itself contain a dictionary. Now this dictionary contains two keys the email and also the password. The values for each of these are pretty self-explanatory. It&#x27;s the email that we got from this line of code, and the password is the password that we got from this line of code. Now that we&#x27;ve created this new dictionary called New Data, well, that is what we&#x27;re going to use to dump into our JSON file. Let&#x27;s go ahead and dump our new data. So that&#x27;s the first input. Now the next input is going to be the data file that we want to put it into. So that is going to be the file that we opened up inside this line. Let&#x27;s provide our data file as the location to dump this data. And now if I go ahead and hit run and I create a new entry for my Amazon website password, generate the password, hit add. Then you can see that we&#x27;ve got our brand new data JSON file that&#x27;s just been created. So just as a quick reminder, when you open a file in write mode, if that file doesn&#x27;t exist, it will actually create it. So we&#x27;ve created this new file data dot JSON. And we&#x27;ve dumped the data that we entered into that file. So now if I open this up you can see we&#x27;ve got a JSON format in here. And we&#x27;ve got our Amazon website and all of its associated data. Notice how this data is not very easy to read for a human. So we can actually improve that by adding one other argument. And it&#x27;s an argument called indent. This we can provide the number of spaces to indent all the JSON data so that it becomes much easier to read. So I&#x27;m going to go ahead and delete everything that&#x27;s in here. And I&#x27;m going to run the code again. And this time once I save this data into my data dot JSON, you can see everything is all indented and it&#x27;s much easier for the human to read. Now that we&#x27;ve seen how we can write data to a JSON file, the next step I want to show you is how you can load data from the JSON file, or how to read from it. To read JSON data, we use the JSON module and we call the load method. So I&#x27;m going to go ahead and comment out this line of code. And now that I&#x27;ve actually got some data in my JSON data file, I&#x27;m going to try and read from it. We call the JSON dot load method and we pass in the file path. Or you can see here the PHP. So that&#x27;s the only required input. The file that we&#x27;re going to pass in is our data file which we opened up over here. And what we&#x27;re going to do is we&#x27;re going to change this from write mode to read mode. Once we&#x27;ve loaded up the data we&#x27;re going to save it inside a variable called data. And then I&#x27;m just going to print it out. So when I hit run right now and I simply add any sort of gobbledygook. But as long as I can hit the add button, it&#x27;s going to trigger this part of the code. And now if we take a look inside our console, you can see that data is being printed down here, but without any of these indentations or any of the formatting. Instead, what that load method does is it essentially takes this JSON data and converts it into a Python dictionary. So this data, if we actually do a type check on it, you can see has a type of dictionary. This is just a normal Python dictionary. So essentially we can use Json.dumps and Json.load to serialize and deserialize from JSON data to Python dictionaries. And it allows us that free interchange of information. We change it into a JSON to store it, and then we take it out of storage and turn it into a Python dictionary to easily work with it in our code. Now that we&#x27;ve seen how to write, how to read, the last thing I want to show you is how to update data. Because if we have a new piece of data come in, we want to add to this JSON, but we don&#x27;t want to overwrite it. But we also don&#x27;t want to just append to the end of it. Because you can imagine if we had something that we just appended to the end. That&#x27;s not a valid data structure. And that&#x27;s why we&#x27;re getting all of these errors. Instead of doing it manually, we&#x27;re going to use the built in method JSON dot update. And here&#x27;s how it works. So we&#x27;ve already got some data inside our data dot JSON file. And what we want to do is we want to take that data and we want to update what&#x27;s existing in there with the new data that&#x27;s being passed in. To do that, we first have to load up the data. So I&#x27;m going to uncomment this line. And once we&#x27;ve gotten hold of the data we&#x27;re going to say data dot update. And we update it with the new data. What&#x27;s happened so far is we have our data loaded up into a dictionary. We use the update method to update that dictionary with some new piece of data. Now, the next thing we want to do is we want to actually write that data back into this file. So we have to use this line of code using JSON dump. But instead of dumping the new data we&#x27;re going to dump that data that we updated right here. Essentially what we&#x27;re doing here is a three step approach. We&#x27;re reading the old data. We&#x27;re updating old data with new data. And we&#x27;re finally saving the updated data. Here we&#x27;ve got two things happening. We&#x27;ve got reading and we&#x27;ve got writing. So we can split this code into two sections. First we open the file in read mode. We get hold of the data, we update the data, and then we open up the file again. But in this case we open it in write mode. This time we&#x27;re going to dump the data that we&#x27;ve updated over here into that data file using this particular indent format. And once we&#x27;ve written to file and we delete all the text from our website and password entries, and these lines of code now replace our previous functionality where we were saving just plain text to our data dot text. Let&#x27;s go ahead and run it and test it out. Currently we have a single entry inside our data dot JSON. So if we go ahead and add a new entry say for eBay and we generate password, click add. If we go back to our data dot JSON, you can see how it&#x27;s now been updated. This entire JSON has now been updated to have two entries Amazon and eBay. It didn&#x27;t just appended it actually added it into the dictionary. And that&#x27;s all thanks to that update method. and once it updated that data, then we told it to save the updated data back into the data file and wipe all of the previous data. So now that we&#x27;re able to read, write, and update JSON data, the next step is to think about what situations might this fail? And one of the biggest problems we have at the moment is we&#x27;re trying to open up this file, load up the data and update it. But if we actually had no data in here, say the first time we ran our program, or indeed if we didn&#x27;t actually have this data dot JSON file, then you can see that the first time I run it and I try to save a password. Then it&#x27;s going to crash. And we get some exceptions being thrown. But we know all about exceptions now. So in the next lesson that&#x27;s what we&#x27;re going to tackle for all of that and more. Head over to the next lesson.

---

### Key Concepts

| # | Concept | Description |
|---|---------|-------------|
| 1 | **if/elif/else conditionals** | Introduced/used in this lecture |
| 2 | **Module imports** | Introduced/used in this lecture |
| 3 | **JSON data handling** | Introduced/used in this lecture |

---

### Next Steps

The goal is to take our password manager to the next level. And in order to do that, we need to add a search functionality. It&#x27;s not good enough just to look through the data file anymore. What we want to be able to do is to type a website into the website entry, and then hit search, and we get a pop up showing us our email and the password that was saved for that website. So this is the goal. But where we&#x27;re currently at is we have a text file which contains the name of the website, the email and the password. This is very difficult to search and this is a really terrible format to work with. So we&#x27;re going to be levelling up our data storage in our password manager. And we&#x27;re going to switch from saving data straight to a text file to a fancier data format, which is called JSON. Nope. This is not the latest, hottest DJ on the block. Lock. It&#x27;s actually JSON, which stands for JavaScript Object Notation. This was something that was designed originally for JavaScript, but because it has such a simple structure and it&#x27;s so easy to understand and work with that it&#x27;s been adopted by many, many different fields, including Python. This is probably one of the most popular ways of transferring data, especially when you&#x27;re transferring data across the internet. So this is what we&#x27;re going to be learning about. And for those of you looking at this slide, you might have already realized that it&#x27;s kind of similar to the dictionaries that we&#x27;ve been working with in Python. Right? A JSON is essentially composed of a bunch of nested lists and dictionaries, and it has that key value pair data structure. This is essentially what we&#x27;re aiming for, so that we can store our data in this format. And we&#x27;ll be able to easily load up this data and search through it for the particular website that we want the information for. To work with JSON data in Python, we can use the inbuilt JSON library, and we&#x27;re going to use it to write, read and update data to a JSON file. To begin, I&#x27;m going to simplify our code a little bit. I&#x27;m going to get rid of this message box which asks the user okay or cancel. We&#x27;re just going to assume that once they click add, then they&#x27;re happy with the email and password. So now that we&#x27;ve cut down that little bit of code, it makes this a little bit easier to understand. All that we&#x27;re doing here is we&#x27;re checking to make sure that the length of the website entry and the length of the password entry is equal to zero. And you might still have the length checking for the email entry. But remember that at the end of previous lessons, we actually inserted a default value so we can actually skip that check as well if you wanted to. The reason why we want all code as simple as possible is so that when we&#x27;re working with the JSON and we&#x27;re learning about how to work with it, as long as everything else is simple, then we can focus on the new things that we&#x27;re learning. In this case, instead of opening a data dot txt file, I&#x27;m going to change the data format to a dot JSON and instead of using the append mode, I&#x27;m going to use the write mode because I&#x27;m going to be writing to this data dot JSON file. Now, in order to write to a JSON file, we don&#x27;t use this line of code. Instead we use a method called JSON dot dump. And that of course requires us to import the JSON module, which should be inbuilt to Python so you don&#x27;t actually have to install it. Now, once we&#x27;ve imported our JSON module we can say JSON dot dump. And this takes a number of inputs. But the most important are the things that you want to dump and the file that you want to dump it to. The data that we want to put in here should go in as a dictionary. Essentially, what we want to create is a new dictionary, which I&#x27;ll call new data. And it&#x27;s going to be a nested dictionary. So the first level key is going to be the website, because this is what we&#x27;re going to be searching through eventually. And the website is going to itself contain a dictionary. Now this dictionary contains two keys the email and also the password. The values for each of these are pretty self-explanatory. It&#x27;s the email that we got from this line of code, and the password is the password that we got from this line of code. Now that we&#x27;ve created this new dictionary called New Data, well, that is what we&#x27;re going to use to dump into our JSON file. Let&#x27;s go ahead and dump our new data. So that&#x27;s the first input. Now the next input is going to be the data file that we want to put it into. So that is going to be the file that we opened up inside this line. Let&#x27;s provide our data file as the location to dump this data. And now if I go ahead and hit run and I create a new entry for my Amazon website password, generate the password, hit add. Then you can see that we&#x27;ve got our brand new data JSON file that&#x27;s just been created. So just as a quick reminder, when you open a file in write mode, if that file doesn&#x27;t exist, it will actually create it. So we&#x27;ve created this new file data dot JSON. And we&#x27;ve dumped the data that we entered into that file. So now if I open this up you can see we&#x27;ve got a JSON format in here. And we&#x27;ve got our Amazon website and all of its associated data. Notice how this data is not very easy to read for a human. So we can actually improve that by adding one other argument. And it&#x27;s an argument called indent. This we can provide the number of spaces to indent all the JSON data so that it becomes much easier to read. So I&#x27;m going to go ahead and delete everything that&#x27;s in here. And I&#x27;m going to run the code again. And this time once I save this data into my data dot JSON, you can see everything is all indented and it&#x27;s much easier for the human to read. Now that we&#x27;ve seen how we can write data to a JSON file, the next step I want to show you is how you can load data from the JSON file, or how to read from it. To read JSON data, we use the JSON module and we call the load method. So I&#x27;m going to go ahead and comment out this line of code. And now that I&#x27;ve actually got some data in my JSON data file, I&#x27;m going to try and read from it. We call the JSON dot load method and we pass in the file path. Or you can see here the PHP. So that&#x27;s the only required input. The file that we&#x27;re going to pass in is our data file which we opened up over here. And what we&#x27;re going to do is we&#x27;re going to change this from write mode to read mode. Once we&#x27;ve loaded up the data we&#x27;re going to save it inside a variable called data. And then I&#x27;m just going to print it out. So when I hit run right now and I simply add any sort of gobbledygook. But as long as I can hit the add button, it&#x27;s going to trigger this part of the code. And now if we take a look inside our console, you can see that data is being printed down here, but without any of these indentations or any of the formatting. Instead, what that load method does is it essentially takes this JSON data and converts it into a Python dictionary. So this data, if we actually do a type check on it, you can see has a type of dictionary. This is just a normal Python dictionary. So essentially we can use Json.dumps and Json.load to serialize and deserialize from JSON data to Python dictionaries. And it allows us that free interchange of information. We change it into a JSON to store it, and then we take it out of storage and turn it into a Python dictionary to easily work with it in our code. Now that we&#x27;ve seen how to write, how to read, the last thing I want to show you is how to update data. Because if we have a new piece of data come in, we want to add to this JSON, but we don&#x27;t want to overwrite it. But we also don&#x27;t want to just append to the end of it. Because you can imagine if we had something that we just appended to the end. That&#x27;s not a valid data structure. And that&#x27;s why we&#x27;re getting all of these errors. Instead of doing it manually, we&#x27;re going to use the built in method JSON dot update. And here&#x27;s how it works. So we&#x27;ve already got some data inside our data dot JSON file. And what we want to do is we want to take that data and we want to update what&#x27;s existing in there with the new data that&#x27;s being passed in. To do that, we first have to load up the data. So I&#x27;m going to uncomment this line. And once we&#x27;ve gotten hold of the data we&#x27;re going to say data dot update. And we update it with the new data. What&#x27;s happened so far is we have our data loaded up into a dictionary. We use the update method to update that dictionary with some new piece of data. Now, the next thing we want to do is we want to actually write that data back into this file. So we have to use this line of code using JSON dump. But instead of dumping the new data we&#x27;re going to dump that data that we updated right here. Essentially what we&#x27;re doing here is a three step approach. We&#x27;re reading the old data. We&#x27;re updating old data with new data. And we&#x27;re finally saving the updated data. Here we&#x27;ve got two things happening. We&#x27;ve got reading and we&#x27;ve got writing. So we can split this code into two sections. First we open the file in read mode. We get hold of the data, we update the data, and then we open up the file again. But in this case we open it in write mode. This time we&#x27;re going to dump the data that we&#x27;ve updated over here into that data file using this particular indent format. And once we&#x27;ve written to file and we delete all the text from our website and password entries, and these lines of code now replace our previous functionality where we were saving just plain text to our data dot text. Let&#x27;s go ahead and run it and test it out. Currently we have a single entry inside our data dot JSON. So if we go ahead and add a new entry say for eBay and we generate password, click add. If we go back to our data dot JSON, you can see how it&#x27;s now been updated. This entire JSON has now been updated to have two entries Amazon and eBay. It didn&#x27;t just appended it actually added it into the dictionary. And that&#x27;s all thanks to that update method. and once it updated that data, then we told it to save the updated data back into the data file and wipe all of the previous data. So now that we&#x27;re able to read, write, and update JSON data, the next step is to think about what situations might this fail? And one of the biggest problems we have at the moment is we&#x27;re trying to open up this file, load up the data and update it. But if we actually had no data in here, say the first time we ran our program, or indeed if we didn&#x27;t actually have this data dot JSON file, then you can see that the first time I run it and I try to save a password. Then it&#x27;s going to crash. And we get some exceptions being thrown. But we know all about exceptions now. So in the next lesson that&#x27;s what we&#x27;re going to tackle for all of that and more. Head over to the next lesson.
